const API = "http://localhost:3001";

const endpoints = {
  teams: `${API}/teams`,
  standings: `${API}/standings`,
  players: `${API}/players`,
  favorites: `${API}/favorites`,
};

export default endpoints;
